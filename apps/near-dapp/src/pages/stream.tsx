import React from 'react';

import {StreamPageContent} from '~/entities/stream';

import {Layout} from '@roketo/core/ui/components/layout';

export const StreamPage = () => (
  <Layout>
    <StreamPageContent />
  </Layout>
);
